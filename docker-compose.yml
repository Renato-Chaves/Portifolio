services:
  frontend:
    image: node:25-alpine
    container_name: portifolio
    working_dir: /app
    volumes:
      - ./portifolio:/app
    command: sh -c "npm install && npm run dev"
    expose:
      - "3000"